# frozen_string_literal: true

RSpec.describe Calypso do
  it "has a version number" do
    expect(Calypso::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
